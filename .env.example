# Local API URL for Vite frontend
VITE_API_URL=http://localhost:4000

# SMTP / email settings for Nodemailer
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
